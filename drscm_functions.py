import csv

def drs_fetch(db, key, criteria):
    with open(file_project) as csvfile:
        condlist = [x < 3, x > 5]
        choicelist = [x]
        alldata = list(reader)
        csvfile.close()
    return alldata

>>> x = np.arange(10)
>>>
>>>
>>> np.select(condlist, choicelist)
array([ 0,  1,  2, ..., 49, 64, 81])